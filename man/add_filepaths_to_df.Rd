% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rows_to_files.R
\name{add_filepaths_to_df}
\alias{add_filepaths_to_df}
\title{Add File Paths to a Data Frame}
\usage{
add_filepaths_to_df(
  data_df,
  id_col_name = "id",
  path,
  file_prefix,
  extension = ".qs2"
)
}
\arguments{
\item{data_df}{A data.frame or tibble containing rows for which paths are
required.}

\item{id_col_name}{Character. Name of the column in \code{data_df} that holds
unique identifiers. Defaults to "id".}

\item{path}{Character. Directory where the files reside.}

\item{file_prefix}{Character. String to prepend before the identifier when
forming the file name.}

\item{extension}{Character. File extension to append; defaults to ".qs2".}
}
\value{
A data frame identical to \code{data_df} but with an added column
\code{file_path} containing the constructed file paths.
}
\description{
Appends a new column \code{file_path} to the supplied data frame. Each entry is
constructed by combining a directory path, an optional file prefix, the value
from the identifier column, and a file extension.
}
\examples{
  df <- tibble::tibble(id = 1:3)
  add_filepaths_to_df(df, id_col_name = "id", path = file.path("data_in", "files"),
                      file_prefix = "sample")
}
